{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run main",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main_aircraft_agent.py",
            "console": "integratedTerminal"
        }
    ]
}